# dados-clientes/ — pasta LOCAL, nunca vai pro git

Suba aqui planilhas e extrações de base de cliente (`.csv`, `.xlsx`, `.xls`,
`.ods`, `.json`, `.txt`, `.pdf`, `.zip`, ...) para eu analisar.

O `.gitignore` ignora **todo** o conteúdo desta pasta — só este README é
versionado. Este repositório é PÚBLICO: nome, telefone, e-mail e faturamento
de aluno/cliente não podem entrar no histórico do git em nenhuma hipótese.

Sugestão de nome de arquivo: `cliente-mes-ano.xlsx` (ex.: `ct-soares-set-2026.xlsx`).
